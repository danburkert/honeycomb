PROJECT(HONEYCOMB_TEST)
SET(UNIT_TEST_SOURCES "${HONEYCOMB_ROOT}/Row.cc")
ADD_EXECUTABLE(unit-test RowTest.cc ${UNIT_TEST_SOURCES})

MESSAGE("CMAKE_PROJECT_NAME: ${CMAKE_PROJECT_NAME}")

ENABLE_TESTING()
ADD_TEST(HONEYCOMB_TEST unit-test)
